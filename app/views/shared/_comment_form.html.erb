<div class="comment-form">
  <%= form_for :comment, url: comments_path do |f| %>
    <%= f.hidden_field :post_id, value: object.id %>
    <%= f.text_area :content, placeholder: "Talk smack..." %>
    <div><%= f.submit "Comment", class: "btn btn-primary" %></div>
  <% end %>
</div>
