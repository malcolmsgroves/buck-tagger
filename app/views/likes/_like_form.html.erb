<!-- When more advanced, add AJAX to automatically update
     the like/unlike buttons -->
<div id="like_form">
<% if current_user.like?(object) %>
  <%= render 'likes/unlike', object: object %>
<% else %>
  <%= render 'likes/like', object: object %>
<% end %>
</div>
